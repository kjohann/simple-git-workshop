/**
 * Valid CLI flags for task scripts
 */
export const CLI_FLAGS = {
  FORCE: '--force',
  CLEAN: '--clean',
} as const;

/**
 * Standard filename for task instructions
 */
export const INSTRUCTIONS_FILE_NAME = 'Instructions.md';
